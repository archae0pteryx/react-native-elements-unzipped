import React from 'react'
import { StyleSheet } from 'react-native'
import { Tile as RNETile } from 'react-native-elements'

export default function Tile (props) {
  return <RNETile {...props} />
}

const styles = StyleSheet.create({
})
